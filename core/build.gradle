group = 'net.techcable.sonarpet'
description = 'SonarPet'
dependencies {
    shade(project(':api'))
    for (version in gradle.ext.nmsVersions) {
        shade(project(":nms-$version")) {
            exclude module: "craftbukkit"
        }
    }
}
